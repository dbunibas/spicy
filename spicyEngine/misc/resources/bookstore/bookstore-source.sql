create table personset (
	pname varchar(50)
);

create table writesset (
	pname varchar(50),
	bid varchar(50)
);

create table bookset (
	bid varchar(50)
);

create table soldatset (
	bid varchar(50),
	sid varchar(50)
);

create table bookstoreset (
	sid varchar(50)
);
