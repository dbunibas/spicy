CREATE TABLE SCity(
cityName varchar(255),
region varchar(255)
);

CREATE TABLE SPersonCar(
personName varchar(255),
carModel varchar(255)
);

CREATE TABLE SPersonCity(
personName varchar(255),
cityName varchar(255)
);

