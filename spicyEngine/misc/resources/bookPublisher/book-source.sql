create table ibdbookset (
	title varchar(50)
);

create table locset (
	title varchar(50),
	publisher varchar(50)
);

create table iblbookset (
	title varchar(50),
	pubid varchar(50)
);

create table iblpublisherset (
	id varchar(50),
	name varchar(50)
);
