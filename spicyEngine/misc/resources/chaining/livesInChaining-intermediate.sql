create table Home (
    homeName varchar(200),
    homeCity varchar(200)
);

create table EmpDept (
    eName varchar(200),
    deptId varchar(200)
);

create table DeptCity (
    dId varchar(200),
    city varchar(200)
);