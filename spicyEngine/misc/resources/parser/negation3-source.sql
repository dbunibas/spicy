create table A (
	a varchar(20),
	b varchar(20),
	c varchar(20)
);

create table B (
	a varchar(20),
	b varchar(20)
);

create table C (
	a varchar(20),
	b varchar(20)
);

create table D (
	a varchar(20),
	b varchar(20)
);

create table E (
	a varchar(20),
	b varchar(20)
);
