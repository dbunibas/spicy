create table HomeSet (
    homeName varchar(200),
    homeCity varchar(200)
);

create table EmpDeptSet (
    eName varchar(200),
    deptId varchar(200)
);

create table DeptCitySet (
    dId varchar(200),
    city varchar(200)
);