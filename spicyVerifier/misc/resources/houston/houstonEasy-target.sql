create table students(
name varchar(255),
surname varchar(255)
);

create table teachers(
name varchar(255),
surname varchar(255)
);

create table tutors(
name varchar(255),
surname varchar(255)
);
